# Codex Instructions

你正在维护 AI Blogger 网站项目。

主要文件是 index.html。

每次完成修改后，请自动执行：

git status
git add .
git commit -m "Update AI Blogger website"
git push origin main

注意：
- 不要删除现有文件
- 不要重新创建整个项目
- 只修改用户要求的内容
- 如果 git push 失败，停止并告诉用户错误原因